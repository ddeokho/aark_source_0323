<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TreeNodeStyleCustom">
        <item name="android:paddingLeft">20dp</item>
    </style>

    <style name="TreeNodeStyleDivided">
        <item name="android:showDividers">middle</item>
        <item name="android:divider">@android:drawable/divider_horizontal_bright</item>
    </style>

    <!-- Theme.AppCompat.Light.Dialog -->
    <style name="AlertDialogMyapps" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorAccent</item> <!-- #0181C8 -->
        <item name="android:windowMinWidthMajor">@dimen/dialog_min_width_major</item> <!-- 95% -->
        <item name="android:windowMinWidthMinor">@dimen/dialog_min_width_minor</item> <!-- 95% -->
    </style>

    <style name="popupTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/colorPrimary</item> <!-- #0181C8 -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item> <!-- #0181C8 -->
        <item name="colorAccent">@color/colorAccent</item> <!-- #0181C8 -->

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowMinWidthMajor">@dimen/dialog_min_width_major</item> <!-- 95% -->
        <item name="android:windowMinWidthMinor">@dimen/dialog_min_width_minor</item> <!-- 95% -->
    </style>
</resources>
